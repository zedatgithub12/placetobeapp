//import liraries
import React from "react";
import { View } from "react-native";
import TimerComponent from "../Components/Timer";

// create a checkout timer component
const Timer = () => (
  <View>
    <TimerComponent />
  </View>
);

export default Timer;
